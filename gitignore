# Dependencies (ติดตั้งใหม่ได้เองด้วย npm install ไม่ต้อง commit)
node_modules/

# Firebase logs / cache
firebase-debug.log
firebase-debug.*.log
.firebase/

# Environment / secrets (ห้ามหลุดขึ้น GitHub เด็ดขาด)
*.env
serviceAccountKey.json
*-service-account*.json

# OS junk files
.DS_Store
Thumbs.db
